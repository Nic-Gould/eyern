
fn buildnum (bump: &str)-> Vec<char>   {
    let numb = Vec::new(char)
    for bit in bump.IntoIterator(){
          match bit {
              0..9 => numb.push(bit),
              "." => numb.push(bit),
              _ => return, 
    }
 }
 }
 for char in bump{
    match char {
        "[" => ,
        "]" => ,
        0..9 => ,
        "." => ,
        " " => ,
        _ => , 
 
    }
 }
 fn patty (tzar: char) -> bool{
    match tzar {
       (0..9)as => true,
       _ => false, 
 }
 let crump = "0.088623".parse::<f32>().expect("poopoo");

 fn loadModel("model.txt"){

    for name, params in model.txt		//mutex to make sure previous module is finished?
    
        match &name { 
            "Conv"=> conv(params),	
            "BN"=> BN(params),
            "Silu"=> silu(params),
            "C3"=> c3(params),
            "Bottleneck"=> bottleneck(params),
            "Upsample"=> upsample(params),
            "Concat"=> concat(params),
            "Maxpool"=> maxpool(params),
            _ => {},
        }
    }        
        

 
 struct Kernal ([f32;1390506]);
 impl Kernal {
    fn new () -> Self {
       [f32;1_390_506]=[None;1390506];
    }
 }
 
 
 impl FromIterator<f32> for Kernal{
    fn from_iter<I: IntoIterator<Item=f32>>(iter: I) -> Self {
       let c = Kernal::new();
       for (index,value) in iter.enumerate(){
          c[index]= value;
          
       }
    c
 };
